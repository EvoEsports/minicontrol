# Maps manager

```ts
class MapManager {
    // return clone of map objects
    get();
    // same thing as above
    getMaplist();
    // return all uid's of maps
    getUids();
    // gets mapcount
    getMapCount();   
}
```
