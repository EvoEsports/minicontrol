{% extends "core/templates/widget.xml.twig" %}

{% block content %}
    <label pos="{{size.width / 2}} -{{size.height / 2}}" z-index="2" size="{{size.width}} {{size.height}}" valign="center2" halign="center" textsize="0.5" text="$fff回 ${{colors.title_fg}}Emotes" focusareacolor1="{{colors.title_bg}}" focusareacolor2="{{colors.highlight}}" action="{{actions['openWidget']}}"/>
{% endblock %}
